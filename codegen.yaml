schema: ${NEXT_PUBLIC_GRAPHQL_HOST}
documents: "src/graphql/queries.ts"
generates:
  ./src/graphql/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher:
          func: '@/graphql/client#fetcher'